spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: false
        show_sql: false
  web:
    error:
      include-message: never
      include-stacktrace: never
      include-exception: false
logging:
  level:
    root: WARN
    swyp12.team9: INFO
    org.hibernate.SQL: WARN