spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true

  devtools:
    livereload:
      enabled: true
    restart:
      enabled: true

  security:
    oauth2:
      client:
        registration:
          # OAuth2 : NAVER registration
          naver:
            client-name: naver
            client-id: gi8mBKLe1AMgsPonHkop
            client-secret: JUJrwwUG9p
            redirect-uri: http://localhost:8080/login/oauth2/code/naver
            authorization-grant-type: authorization_code
            scope:
              - name
              - email
              - nickname

          # OAuth2 : Google registration
          google:
            client-name: google
            client-id: 1012749788418-e7krmtd429p7r48i50oe6ort6ovvnbsu.apps.googleusercontent.com
            client-secret: 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
            redirect-uri: http://localhost:8080/login/oauth2/code/google
            authorization-grant-type: authorization_code
            scope:
              - profile
              - email

          # OAuth2 : Kakao registration
          kakao:
            client-name: kakao
            client-id: ${KAKAO_CLIENT_ID:e4e1182d8d6a10bcafec3c88ef023999}
            client-secret: ${KAKAO_CLIENT_SECRET:}
            redirect-uri: http://localhost:8080/login/oauth2/code/kakao
            authorization-grant-type: authorization_code
            client-authentication-method: client_secret_post
            scope:
              - profile_nickname
              #- account_email
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response
          kakao:
            authorization-uri: https://kauth.kakao.com/oauth/authorize
            token-uri: https://kauth.kakao.com/oauth/token
            user-info-uri: https://kapi.kakao.com/v2/user/me
            user-name-attribute: id
<<<<<<< HEAD
  #          google:
  #            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
  #            token-uri: https://oauth2.googleapis.com/token
  #            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
  #            user-name-attribute: sub
  #google: 생략 가능
  # Sring Security가 이미 알고있는 provider들
  # google, facebook, github
  # 그래서 따로 google은 추가안해도 된다.

  ai:
    openai:
      api-key: ${OPENAI_API_KEY} # 환경변수로 설정
      chat:
        options:
          model: gpt-4o-mini
      embedding:
        options:
          model: text-embedding-3-small
  elasticsearch:
    uris: http://localhost:9200
=======
  #          google:
  #            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
  #            token-uri: https://oauth2.googleapis.com/token
  #            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
  #            user-name-attribute: sub
  #google: 생략 가능
  # Sring Security가 이미 알고있는 provider들
  # google, facebook, github
  # 그래서 따로 google은 추가안해도 된다.

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
      embedding:
        options:
          model: text-embedding-3-small
  elasticsearch:
    uris: http://localhost:9200

# Web Scraper Configuration
scraper:
  apify:
    api-key: ${APIFY_API_KEY}
    instagram-actor-id: apify/instagram-scraper
  timeout-seconds: 5
  retry-attempts: 1
# Google Console에 등록된 Redirect URI와 불일치 → OAuth2AuthenticationException(GlobalExceptionHandler.java) 발생
>>>>>>> a756d0e (refactor : GlobalExceptionHandler로 예외처리 통합)

logging:
  level:
    root: INFO
    swyp12.team9: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.springframework.web: DEBUG
